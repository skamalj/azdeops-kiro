{
  "mcpServers": {
    "azure-devops-core": {
      "command": "npx",
      "args": [
        "tsx",
        "https://raw.githubusercontent.com/skamalj/azdeops-kiro/main/mcp-server/index.ts"
      ],
      "env": {
        "AZURE_DEVOPS_ORG_URL": "${AZURE_DEVOPS_ORG_URL}",
        "AZURE_DEVOPS_PROJECT": "${AZURE_DEVOPS_PROJECT}",
        "AZURE_DEVOPS_PAT": "${AZURE_DEVOPS_PAT}"
      }
    }
  }
}